pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh '''
                   echo "BUILD NUMBER: ${BUILD_NUMBER}"
                   cd dt-ejb
                   mvn clean install test
                   cd ../Rest
                   mvn clean install test
                   cd ../web
                   mvn clean install test
                   '''
             }      
        }
    }
}
